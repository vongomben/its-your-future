# Override locale: usa con
#   bundle exec jekyll serve --config _config.yml,_config_dev.yml
# per aprire http://localhost:4000/ (baseurl già vuoto in _config.yml)
baseurl: ""
url: "http://localhost:4000"
